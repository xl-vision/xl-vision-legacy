# 编辑器

::: demo 基本用法
基本用法

```jsx
import {Editor} from '@xl-vision/xl-vision'
export default () => {
  return (
    <Editor/>
  )
}
```

:::

## Editor API

| 参数   | 说明                                       | 类型            | 可选值 | 默认值 | 是否必填
| ------ | ------------------------------------------ | --------------- | ------ | ------ | --- |
