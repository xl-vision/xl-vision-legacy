# 按钮

基础的交互组件

:::demo 基础用法

通过`theme`、`variant`指定按钮的样式，按钮中也可以添加[Icon](#/icon)。

```jsx
import React from 'react'
import { Button } from 'xl-vision'
import { Search } from 'xl-vision/lib/icon'

export default () => {
  return (
    <div className='wrapper'>
      <div className='column'>
        <Button>Default</Button>
        <Button theme='primary'>Primary</Button>
        <Button theme='secondary'>Secondary</Button>
        <Button theme='error'>Error</Button>
        <Button theme='info'>Info</Button>
        <Button theme='warning'>Warning</Button>
        <Button theme='success'>Success</Button>
      </div>
      <div className='column'>
        <Button variant='outlined'>Default</Button>
        <Button variant='outlined' theme='primary'>
          Primary
        </Button>
        <Button variant='outlined' theme='secondary'>
          Secondary
        </Button>
        <Button variant='outlined' theme='error'>
          Error
        </Button>
        <Button variant='outlined' theme='info'>
          Info
        </Button>
        <Button variant='outlined' theme='warning'>
          Warning
        </Button>
        <Button variant='outlined' theme='success'>
          Success
        </Button>
      </div>
      <div className='column'>
        <Button variant='text'>Default</Button>
        <Button variant='text' theme='primary'>
          Primary
        </Button>
        <Button variant='text' theme='secondary'>
          Secondary
        </Button>
        <Button variant='text' theme='error'>
          Error
        </Button>
        <Button variant='text' theme='info'>
          Info
        </Button>
        <Button variant='text' theme='warning'>
          Warning
        </Button>
        <Button variant='text' theme='success'>
          Success
        </Button>
      </div>
    </div>
  )
}
```

```scss
.column::deep .xl-button {
  margin: 0.5rem;
}
```

:::

:::demo 添加图标

添加图标可以使按钮更加醒目

```jsx
import React from 'react'
import { Button } from 'xl-vision'
import { Trash, Close } from 'xl-vision/lib/icon'

export default () => {
  return (
    <div>
      <div className='column'>
        <Button theme='primary' prefixIcon={<Trash />}>
          Button
        </Button>
        <Button theme='primary' variant='outlined' prefixIcon={<Trash />}>
          Button
        </Button>
        <Button theme='primary' variant='text' prefixIcon={<Trash />}>
          Button
        </Button>
        <Button theme='primary' prefixIcon={<Trash />} />
      </div>
      <div className='column'>
        <Button theme='primary' suffixIcon={<Close />}>
          Button
        </Button>
        <Button theme='primary' variant='outlined' suffixIcon={<Close />}>
          Button
        </Button>
        <Button theme='primary' variant='text' suffixIcon={<Close />}>
          Button
        </Button>
        <Button theme='primary' suffixIcon={<Close />} />
      </div>
    </div>
  )
}
```

```scss
.column::deep .xl-button {
  margin: 0.5rem;
}
```

:::

:::demo 圆角按钮

可以通过`round`设置圆角按钮

```jsx
import React from 'react'
import { Button } from 'xl-vision'
import { Trash, Close } from 'xl-vision/lib/icon'

export default () => {
  return (
    <div>
      <div className='column'>
        <Button theme='primary' round>
          contained
        </Button>
        <Button theme='primary' round variant='outlined'>
          outlined
        </Button>
        <Button theme='primary' round variant='text'>
          text
        </Button>
      </div>
      <div className='column'>
        <Button theme='primary' prefixIcon={<Trash />} round>
          contained
        </Button>
        <Button theme='primary' prefixIcon={<Trash />} round variant='outlined'>
          outlined
        </Button>
        <Button theme='primary' prefixIcon={<Trash />} round variant='text'>
          text
        </Button>
      </div>
      <div className='column'>
        <Button theme='primary' suffixIcon={<Close />} round>
          contained
        </Button>
        <Button theme='primary' suffixIcon={<Close />} round variant='outlined'>
          outlined
        </Button>
        <Button theme='primary' suffixIcon={<Close />} round variant='text'>
          text
        </Button>
      </div>
      <div className='column'>
        <Button theme='primary' prefixIcon={<Trash />} round></Button>
        <Button theme='primary' prefixIcon={<Trash />} round variant='outlined'></Button>
        <Button theme='primary' prefixIcon={<Trash />} round variant='text'></Button>
      </div>
      <div className='column'>
        <Button theme='primary' suffixIcon={<Trash />} round></Button>
        <Button theme='primary' suffixIcon={<Trash />} round variant='outlined'></Button>
        <Button theme='primary' suffixIcon={<Trash />} round variant='text'></Button>
      </div>
    </div>
  )
}
```

```scss
.column::deep .xl-button {
  margin: 0.5rem;
}
```

:::

:::demo 禁止状态

通过`disabled`禁止按钮

```jsx
import React from 'react'
import { Button } from 'xl-vision'
import { Trash, Close } from 'xl-vision/lib/icon'

export default () => {
  return (
    <div className='container'>
      <div className='column'>
        <Button disabled theme='primary'>
          contained
        </Button>
        <Button disabled theme='primary' variant='outlined'>
          outlined
        </Button>
        <Button disabled theme='primary' variant='text'>
          text
        </Button>
      </div>
      <div className='column'>
        <Button disabled round theme='primary'>
          contained
        </Button>
        <Button disabled round theme='primary' variant='outlined'>
          outlined
        </Button>
        <Button disabled round theme='primary' variant='text'>
          text
        </Button>
      </div>
      <div className='column'>
        <Button disabled theme='primary' prefixIcon={<Trash />}>
          Button
        </Button>
        <Button disabled theme='primary' suffixIcon={<Close />}>
          Button
        </Button>
        <Button disabled round theme='primary' variant='outlined' prefixIcon={<Close />} />
      </div>
    </div>
  )
}
```

```scss
.column::deep .xl-button {
  margin: 0.5rem;
}
```

:::

:::demo 取消阴影

对于`variant`为`contained`类型的按钮，可以通过`disableElevation`取消阴影。

```jsx
import React from 'react'
import { Button } from 'xl-vision'

export default () => {
  return (
    <div className='column'>
      <Button disableElevation>Default</Button>
      <Button disableElevation theme='primary'>
        Primary
      </Button>
      <Button disableElevation theme='secondary'>
        Secondary
      </Button>
      <Button disableElevation theme='error'>
        Error
      </Button>
      <Button disableElevation theme='info'>
        Info
      </Button>
      <Button disableElevation theme='warning'>
        Warning
      </Button>
      <Button disableElevation theme='success'>
        Success
      </Button>
    </div>
  )
}
```

```scss
.column::deep .xl-button {
  margin: 0.5rem;
}
```

:::

:::demo 加载中

加载中的按钮不会响应用户操作，适合在某些需要延时完成的操作，可以避免用户的多次点击。

```jsx
import React from 'react'
import { Button } from 'xl-vision'
import { Trash, Close } from 'xl-vision/lib/icon'

const Loading = ({ children }) => {
  const [loading, setLoading] = React.useState(false)
  const timerRef = React.useRef()

  React.useEffect(() => {
    return () => {
      clearTimeout(timerRef.current)
    }
  }, [])

  const onClick = React.useCallback(() => {
    setLoading(true)
    timerRef.current = setTimeout(() => {
      setLoading(false)
    }, 3000)
  }, [])

  return React.cloneElement(children, {
    loading,
    onClick
  })
}

export default () => {
  return (
    <div className='wrapper'>
      <div className='column'>
        <Loading>
          <Button theme='primary'>Button</Button>
        </Loading>
        <Loading>
          <Button theme='primary' variant='outlined'>
            Button
          </Button>
        </Loading>
        <Loading>
          <Button theme='primary' variant='text'>
            Button
          </Button>
        </Loading>
      </div>
      <div className='column'>
        <Loading>
          <Button theme='primary' prefixIcon={<Trash />}>
            Button
          </Button>
        </Loading>
        <Loading>
          <Button theme='primary' variant='outlined' prefixIcon={<Trash />}>
            Button
          </Button>
        </Loading>
        <Loading>
          <Button theme='primary' variant='text' prefixIcon={<Trash />}>
            Button
          </Button>
        </Loading>
      </div>
      <div className='column'>
        <Loading>
          <Button theme='primary' suffixIcon={<Close />}>
            Button
          </Button>
        </Loading>
        <Loading>
          <Button theme='primary' variant='outlined' suffixIcon={<Close />}>
            Button
          </Button>
        </Loading>
        <Loading>
          <Button theme='primary' variant='text' suffixIcon={<Close />}>
            Button
          </Button>
        </Loading>
      </div>
      <div className='column'>
        <Loading>
          <Button round theme='primary' prefixIcon={<Close />} />
        </Loading>
        <Loading>
          <Button round theme='primary' variant='outlined' suffixIcon={<Close />} />
        </Loading>
        <Loading>
          <Button round theme='primary' variant='text' prefixIcon={<Close />} />
        </Loading>
      </div>
    </div>
  )
}
```

```scss
.column::deep .xl-button {
  margin: 0.5rem;
}
```

:::

:::demo 更长的按钮

通过`long`是按钮占据一整行

```jsx
import React from 'react'
import { Button } from 'xl-vision'
import { Trash, Close } from 'xl-vision/lib/icon'

export default () => {
  return (
    <div className='column'>
      <Button long theme='primary'>
        long
      </Button>
      <Button long theme='secondary' prefixIcon={<Trash />}>
        Delete
      </Button>
      <Button long theme='error' suffixIcon={<Close />}>
        Close
      </Button>
    </div>
  )
}
```

```scss
.column::deep .xl-button {
  margin: 0.5rem;
}
```

:::

:::demo 按钮组

将多个相应功能的按钮放在一起形成一个按钮组

```jsx
import React from 'react'
import { Button } from 'xl-vision'
import { Trash, Close } from 'xl-vision/lib/icon'

export default () => {
  return (
    <div>
      <div className='column'>
        <Button.Group theme='primary'>
          <Button>button1</Button>
          <Button disabled>button2</Button>
          <Button>button3</Button>
        </Button.Group>
      </div>
      <div className='column'>
        <Button.Group theme='primary' variant='outlined'>
          <Button suffixIcon={<Close />}></Button>
          <Button disabled>button2</Button>
          <Button>button3</Button>
        </Button.Group>
      </div>
      <div className='column'>
        <Button.Group theme='primary' variant='text'>
          <Button prefixIcon={<Trash />}>button1</Button>
          <Button disabled>button2</Button>
          <Button>button3</Button>
        </Button.Group>
      </div>
      <div className='column'>
        <Button.Group round theme='primary'>
          <Button prefixIcon={<Trash />}></Button>
          <Button disabled>button2</Button>
          <Button>button3</Button>
        </Button.Group>
      </div>
      <div className='column'>
        <Button.Group round theme='primary' variant='outlined'>
          <Button suffixIcon={<Close />}>button1</Button>
          <Button disabled>button2</Button>
          <Button>button3</Button>
        </Button.Group>
      </div>
      <div className='column'>
        <Button.Group round theme='primary' variant='text'>
          <Button prefixIcon={<Trash />}>button1</Button>
          <Button disabled>button2</Button>
          <Button>button3</Button>
        </Button.Group>
      </div>
    </div>
  )
}
```

```scss
.column::deep .xl-button-group {
  margin: 0.5rem;
}
```

:::

:::demo 针对按钮组中某些按钮进行设置

按钮组中的按钮也可以单独设置

```jsx
import React from 'react'
import { Button } from 'xl-vision'

export default () => {
  return (
    <div>
      <div className='column'>
        <Button.Group theme='primary'>
          <Button>button1</Button>
          <Button theme='error'>button2</Button>
          <Button>button3</Button>
        </Button.Group>
      </div>
      <div className='column'>
        <Button.Group variant='outlined' theme='primary'>
          <Button>button1</Button>
          <Button theme='error'>button2</Button>
          <Button>button3</Button>
        </Button.Group>
      </div>
      <div className='column'>
        <Button.Group variant='text' theme='primary'>
          <Button>button1</Button>
          <Button theme='error'>button2</Button>
          <Button>button3</Button>
        </Button.Group>
      </div>
    </div>
  )
}
```

```scss
.column::deep .xl-button-group {
  margin: 0.5rem;
}
```

:::

## Button API

> 其余参数请参考<Link to='BaseButton'>`BaseButton`</Link>

| 参数 | 说明 | 类型 | 可选值 | 默认值 | 是否必填 |
| --- | --- | --- | --- | --- | --- |
| clsPrefix | 自定义类名前缀 | string | - | - | false |
| href | 点击跳转地址，此时 button 行为与 a 标签一致 | string | - | - | false |
| disabled | 禁止按钮 | boolean | - | false | false |
| theme | 设置按钮主题 | string | default &#x007c; primary &#x007c; success &#x007c; warning &#x007c; error &#x007c; secondary | default | false |
| variant | 设置按钮类型 | string | contained &#x007c; outlined &#x007c; text | contained | false |
| long | 设置为长按钮 | boolean | - | false | false |
| prefixIcon | 前置图标 | ReactElement | - | - | false |
| suffixIcon | 后置图标 | ReactElement | - | - | false |
| loading | 设置按钮为加载中 | boolean | - | false | false |
| round | 圆角按钮 | boolean | - | - | false |
| disableElevation | 禁止阴影效果，仅对`variant=contained` | boolean | - | - | false |
| children | 子节点，在指定了`prefixIcon`或`suffixIcon`时可以为空 | ReactNode | - | - | false |

## Group API

> 使用按钮组时，请不要设置`Button`的`round`和`long`属性，否则会导致显示异常。

| 参数 | 说明 | 类型 | 可选值 | 默认值 | 是否必填 |
| --- | --- | --- | --- | --- | --- |
| clsPrefix | 自定义类名前缀 | string | - | - | false |
| round | 指定按钮组中按钮的圆角样式 | boolean | - | false | false |
| disableElevation | 禁止阴影效果，仅对`variant=contained` | boolean | - | - | false |
| disableRipple | 禁止水波纹 | boolean | - | - | false |
| theme | 设置按钮主题 | string | default &#x007c; primary &#x007c; success &#x007c; warning &#x007c; error &#x007c; secondary | default | false |
| variant | 设置按钮类型 | string | contained &#x007c; outlined &#x007c; text | contained | false |
| children | 按钮数组 | Button &#x007c; Button[] | - | - | true |
