import './index.scss'

import * as React from 'react'

export default class Footer extends React.Component {
    render() {
        return (
            <h1 className={'demo'}>Footer</h1>
        )
    }
}